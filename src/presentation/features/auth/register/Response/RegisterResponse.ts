import type { AuthSessionData } from '../../../../../core/network/authTypes';

export type { AuthSessionData };

/** Alias for register success body (same shape as login session). */
export type RegisterSuccessData = AuthSessionData;
